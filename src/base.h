#include <stack>
#include <vector>
#include <string>
#include <stdint.h>
using std::stack;
using std::vector;
using std::string;
extern uint16_t MAX_STATE,MAX_STACK;
extern uint32_t do_mod_p;
vector<string> split(string s,char sign);
class luogu_stack{
    stack<uint32_t>data;
    uint16_t size;
    luogu_stack();
}Three[3];
class order{
    uint8_t name;
    uint32_t args[4];
    bool load(string s);
};
